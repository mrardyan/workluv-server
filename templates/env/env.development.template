# =============================================================================
# GO-SERVER APPLICATION - DEVELOPMENT ENVIRONMENT TEMPLATE
# =============================================================================
# 
# Copy this file to .env.development and fill in your actual values.
# cp templates/env/env.development.template .env.development
#
# SECURITY NOTE: Never commit .env files to version control!
# =============================================================================

# =============================================================================
# RUNTIME-ONLY SECRETS (RUN_TIME SCOPE)
# =============================================================================
# These variables are ONLY available at runtime and are treated as secrets.
# They are NOT embedded in the container image for security.
# =============================================================================

# JWT Secrets (Runtime-Only)
JWT_SECRET=dev-jwt-secret-key-change-in-production
JWT_REFRESH_SECRET=dev-refresh-secret-key-change-in-production

# Email Secrets (Runtime-Only)
EMAIL_SMTP_USERNAME=development-email-smtp-username
EMAIL_SMTP_PASSWORD=development-email-smtp-password

# Database Secrets (Runtime-Only)
# DATABASE_URL is managed by DigitalOcean and injected at runtime
DATABASE_URL=${db.DATABASE_URL}

# External Service Secrets (Runtime-Only)
# Uncomment and configure as needed
# STRIPE_SECRET_KEY=sk_test_...
# TWILIO_AUTH_TOKEN=your-twilio-token
# AWS_ACCESS_KEY_ID=your-aws-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret

# Deployment Secrets (Runtime-Only)
DO_APP_ID=
DO_APP_NAME=go-server-dev

# =============================================================================
# BUILD-TIME CONFIGURATION (RUN_AND_BUILD_TIME SCOPE)
# =============================================================================
# These variables are available during build and runtime.
# They are embedded in the container image for configuration.
# =============================================================================

# Environment Configuration
ENVIRONMENT=development
GO_ENV=development
GIN_MODE=debug

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
HTTP_PORT=8080
SERVER_READ_TIMEOUT=30s
SERVER_WRITE_TIMEOUT=30s
SERVER_IDLE_TIMEOUT=60s
SERVER_MAX_HEADER_BYTES=1048576

# Client Configuration
CLIENT_URL=https://dev.go-server.app

# Database Configuration (Non-sensitive)
DB_SSL_MODE=require
DB_MAX_OPEN_CONNS=10
DB_MAX_IDLE_CONNS=10
DB_CONN_MAX_LIFETIME=5m

# Logging Configuration
LOG_LEVEL=debug
LOG_FORMAT=json
LOG_OUTPUT=stdout
LOG_TIME_FORMAT=2006-01-02T15:04:05Z07:00
LOG_CALLER=false

# Monitoring Configuration
METRICS_ENABLED=true
METRICS_PORT=:9090
HEALTH_CHECK_PATH=/health
READINESS_PATH=/health/ready
LIVENESS_PATH=/health/live
PROMETHEUS_PATH=/metrics

# Feature Flags
FEATURE_USER_REGISTRATION=true
FEATURE_EMAIL_VERIFICATION=true
FEATURE_PASSWORD_RESET=true
FEATURE_MULTI_TENANCY=false
FEATURE_AUDIT_LOGGING=true

# Email Service Configuration (Non-sensitive)
EMAIL_SERVICE_ENABLED=true
EMAIL_SERVICE_PROVIDER=smtp
EMAIL_SMTP_HOST=live.smtp.mailtrap.io
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USE_TLS=true
EMAIL_SMTP_USE_SSL=false
EMAIL_FROM_ADDRESS=noreply@workluv.app
EMAIL_FROM_NAME=Workluv
EMAIL_TEMPLATE_DIR=src/email

# Security Configuration (Non-sensitive)
JWT_EXPIRATION=24h
SESSION_EXPIRATION=168h
BCRYPT_COST=10

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://dev.workluv.app

# Rate Limiting
RATE_LIMIT_REQUESTS=1000
RATE_LIMIT_WINDOW=1m

# =============================================================================
# INTERNATIONALIZATION CONFIGURATION (BUILD-TIME)
# =============================================================================
DEFAULT_TIMEZONE=UTC
DEFAULT_CURRENCY=USD
DEFAULT_LOCALE=en_US
SUPPORTED_LOCALES=en_US,es_ES,fr_FR,de_DE,ja_JP,zh_CN
SUPPORTED_CURRENCIES=USD,EUR,GBP,JPY,CAD,AUD
SUPPORTED_TIMEZONES=UTC,America/New_York,Europe/London,Asia/Tokyo

# =============================================================================
# DEVELOPMENT AND TESTING CONFIGURATION (BUILD-TIME)
# =============================================================================
DEBUG_MODE=true
PROFILING_ENABLED=false
PROFILING_PORT=:6060
TRACE_ENABLED=false

# =============================================================================
# NOTES
# =============================================================================
# This template now clearly separates runtime-only secrets from build-time configuration.
# The deployment script automatically categorizes variables based on their names:
#
# RUNTIME-ONLY (RUN_TIME scope):
# - JWT_SECRET, JWT_REFRESH_SECRET
# - EMAIL_SMTP_USERNAME, EMAIL_SMTP_PASSWORD
# - DATABASE_URL
# - External service API keys
# - DO_APP_ID, DO_APP_NAME
#
# BUILD-TIME (RUN_AND_BUILD_TIME scope):
# - ENVIRONMENT, SERVER_*, LOG_*
# - FEATURE_* flags, CORS_*, RATE_LIMIT_*
# - Non-sensitive configuration
#
# Security Benefits:
# - Runtime-only secrets are never embedded in container images
# - Secrets are injected securely at container startup
# - Build artifacts contain no sensitive information 