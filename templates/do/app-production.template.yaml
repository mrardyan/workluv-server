name: workluv-prod
services:
  - name: workluv
    source_dir: /
    github:
      repo: YOUR_GITHUB_USERNAME/YOUR_REPO_NAME
      branch: main
      deploy_on_push: true
    dockerfile_path: /Dockerfile
    http_port: 8080
    instance_count: 2
    instance_size_slug: apps-s-2vcpu-2gb
    health_check:
      http_path: /health/simple
    envs:
      # Environment variables will be added here by the generator script
      # from .env.production file

databases:
  - name: db
    engine: PG
    version: "15"
    production: true
    cluster_name: workluv-prod-cluster
    db_name: golang_arch_prod
    db_user: golang_arch

domains:
  - domain: api.workluv.app
    type: PRIMARY
    zone: workluv.app

alerts:
  - rule: DEPLOYMENT_FAILED
  - rule: DOMAIN_FAILED

features:
  - buildpack-stack=ubuntu-22

ingress:
  rules:
    - component:
        name: workluv
      match:
        path:
          prefix: /

region: sgp 